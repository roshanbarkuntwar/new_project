import { FrameNewcalenderPipe } from './frame-newcalender.pipe';

describe('FrameNewcalenderPipe', () => {
  it('create an instance', () => {
    const pipe = new FrameNewcalenderPipe();
    expect(pipe).toBeTruthy();
  });
});
