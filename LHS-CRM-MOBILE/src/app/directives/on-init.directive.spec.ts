import { OnInitDirective } from './on-init.directive';

describe('OnInitDirective', () => {
  it('should create an instance', () => {
    const directive = new OnInitDirective();
    expect(directive).toBeTruthy();
  });
});
