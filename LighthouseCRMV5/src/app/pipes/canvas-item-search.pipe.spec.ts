import { CanvasItemSearchPipe } from './canvas-item-search.pipe';

describe('CanvasItemSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CanvasItemSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
