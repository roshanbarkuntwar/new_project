import { LovComboSearchPipe } from './lov-combo-search.pipe';

describe('LovComboSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LovComboSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
