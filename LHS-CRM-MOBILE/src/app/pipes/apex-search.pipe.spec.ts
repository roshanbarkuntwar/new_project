import { ApexSearchPipe } from './apex-search.pipe';

describe('ApexSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ApexSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
