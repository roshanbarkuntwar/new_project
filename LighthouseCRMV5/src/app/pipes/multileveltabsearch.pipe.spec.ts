import { MultileveltabsearchPipe } from './multileveltabsearch.pipe';

describe('MultileveltabsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MultileveltabsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
